import { FeatureDisabledDirective } from './feature-disabled.directive';

describe('FeatureDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
