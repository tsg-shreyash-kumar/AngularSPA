import { FeaturesAccessDirective } from './features-access.directive';

describe('FeaturesAccessDirective', () => {
  //it('should create an instance', () => {
  //  const directive = new FeaturesAccessDirective();
  //  expect(directive).toBeTruthy();
  //});
});
